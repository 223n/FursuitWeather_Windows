# 未決事項とリスク

まだ決まっていない仕様と、確かめていない技術的な前提をまとめます。
2026年9月9日時点の内容です。

## 先に決めること

いま未決の項目はありません。
新しく出てきたら、ここへ書きます。

## 決まったこと

### 配布の形

インストーラーで配ります。
Inno Setup 6で利用者単位の非昇格インストーラーを作り、Windows App SDKのランタイムを連鎖インストールします。
これにより`AppNotificationManager`を使えます。
詳しくは[技術選定](stack.md)の「配布方式」にあります。

当初は「実行ファイルを置くだけ」と通知の質を天秤にかけていました。
しかし分岐点は配布の形ではなく、**Windows App SDKのランタイムを配るかどうか**でした。
self-containedにしても`AppNotificationManager`が依存するSingletonパッケージは付いてこないため、「置くだけ」で通知を成立させる道はもとからありません。

### 小窓の高さ

最前面（`Topmost=True`）を既定にします。
フォーカスは奪わず（`WS_EX_NOACTIVATE`）、全画面やプレゼンテーションの間は自動で退避します。
既定の形は折りたたんだバー（およそ220×44）で、ホバーかクリックで展開します。
設定で「常に手前」「ほかの窓に隠れてよい」「トレイだけ」の3つから選べるようにします。

**訂正**: この文書はかつて「アイコンの手前に置く小窓でよければ、`HWND_BOTTOM`への固定で足ります」と書いていました。
誤りでした。
Windows 11 25H2（build 26200）での実測で、次を確認しました。

- `HWND_BOTTOM`を指定しても`Progman`より下へは行けません。自ウィンドウがidx 302、`Progman`がidx 304でした。`hwndInsertAfter`に`Progman`を明示しても同じです
- したがって`HWND_BOTTOM`の窓は、必ずデスクトップのアイコンの手前になります
- 「デスクトップの表示」の実行中は`Progman`がidx 304から48へ上がり、最背面へ固定した窓は最小化されないまま裏に回って見えなくなります

つまり`HWND_BOTTOM`は「ほかの窓に隠れて見えず、デスクトップを表示したときにも見えない」という挙動です。
目的と正反対のため採りません。

真の壁紙の層も採りません。
既定の状態では壁紙用の`WorkerW`が存在せず、未文書のメッセージ`0x052C`でOSの内部の状態を作り替える必要があります。
`SetParent`はプロセスをまたぐとDPI認識を強制的にリセットするため、Per-Monitor V2の宣言とも噛み合いません。

最前面の窓は「デスクトップの表示」の間も見えたままでした。
Rainmeterが使っている250msのポーリングと補助のウィンドウは不要になります。

`WM_WINDOWPOSCHANGING`のフックも実装しません。
最背面への固定をやめたためです。

### 判定の悪化の定義

**訂正**: この文書はかつて「本体のWeb版に前例がないため、ゼロから決めます」と書いていました。
誤りでした。
本体には前例が3つあります。
比較する値、単位、方向のどれもがそれぞれ違います。

判定の基準は`activityMinutes`にし、再通知の抑制は署名の方式にします。
設計は[通知の設計](notifications.md)にあります。

### 自動更新の扱い

利用者が3つのモードから選べるようにします。
自動で更新する、取得だけ自動で行う、お知らせのみ、の3つです。
既定は「取得だけ自動」です。

更新があるかの確認だけは、どのモードでも自動で行います。
設計は[更新の仕組み](update.md)にあります。

## 確かめていない技術的な前提

| 項目 | 状態 |
| ---- | ---- |
| `AllowsTransparency`の実際の描画コスト | Microsoftの定量的な情報は2006年から2008年のXPとVistaの世代で止まっており、現行の環境の公式な測定値は見つかりませんでした。面積が小さく更新も低頻度なら実害は小さいと**推定**していますが、実測ではありません |
| Windows App SDK 2.4.0の未パッケージWPFでの実績 | **確認済み**（2026年9月9日、Windows 11 25H2 / build 26200）。framework-dependentかつ未パッケージのWPFで`Register(displayName, iconUri)`が成功し、通知を出せました。`HKCU\SOFTWARE\Classes\AppUserModelId`にGUIDのキーが自動で作られ、`DisplayName`・`IconUri`・`CustomActivator`が書かれます。**スタートメニューのショートカットは不要**でした |
| issue #6071の修正の取り込み | self-containedで`Register()`がCOMの例外になる不具合のPR #6725は、2026年9月3日にmainへマージされたものの、まだどのリリースタグにも入っていません。最新の安定版2.4.0は8月13日です。「修正済み」と読まないでください |
| 非昇格での`WindowsAppRuntimeInstall.exe --quiet`の終了コード | インストーラーのソースを読む限り0が返る見込みです。ただし環境の要因で`AddPackageAsync`自体が`0x80070005`を返す可能性があり、実機での確認が要ります |
| 通知のコールドローンチの分岐 | Microsoft Learnの本文とサンプルコードが食い違っています。本文は活性化の種類を`Launch`だとし、コードは`AppNotification`で分岐しています。両方の経路を通るコードを書いてください |
| インストーラーとインストール後のサイズ | 約190MBと約300MBはいずれも推定です。`dotnet publish`の出力を実測していません |
| 未署名のインストーラーをアプリから起動したときのSmartScreenの挙動 | 更新の既定値を「取得だけ自動」にした根拠そのものです。警告が出るなら全自動のモードは成立しません。最優先で実機の確認が要ります |
| 上書きの更新が途中で失敗したときのファイルの残り方 | Innoのロールバックがインストール前から存在したファイルを消さない点は、ソースの読解による結論です。公式のドキュメントに明記がありません |
| `AppMutex`が残っているときの終了コード | ソース上は1と読めますが未確認です |
| `PowerManager.PowerSourceKind`の列挙のメンバー名 | 実装のときに確認します |
| `WindowsAppRuntimeInstall.exe --quiet`の失敗をインストーラーが握り潰さないか | 握り潰すなら`GetCustomSetupExitCode`で見えるようにする設計が別に要ります |
| NSwagがOpenAPI 3.1を読めるか | NSwagの公式READMEは2.0と3.0だけを掲げ、3.1に言及がありません。本体の`openapi.yaml`は3.1.0です。駄目ならKiotaか手書きへ切り替えます |
| Font AwesomeのSVGをWPFのGeometryへ移すときの塗りつぶし規則 | SVGの既定はnonzero、WPFのPath Markup Syntaxの既定はEvenOddです。`fa-ban`などはデータの先頭に`F1`が要ると**推定**していますが、実際に描いて比べていません |
| Modern Standby環境でのスリープ復帰 | `PowerModeChanged`が期待どおり発火するかを一次情報で確認できていません。単調時刻による経過の判定を基本の層に置けば、イベントが届かなくてもtickの周期で復帰できます |
| Windows 11のトレイアイコンの既定 | **確認済み**。通知領域へ追加したアイコンは既定でオーバーフローへ入り、通知領域へ昇格させられるのは利用者だけです。初回の起動時に案内する設計が要ります。同じ文書が「通知領域は、直ちに対処が必要な重要な情報のためのものではない」と述べているため、トレイアイコンを唯一の窓口にしません |
| `AppNotificationScenario.Urgent`の未パッケージのWPFでの実挙動 | `IsUrgentScenarioSupported()`が`True`を返すことは**確認済み**です。ただし実際に「応答不可」を突破するか、許可を求める画面がどう出るかは未確認です |
| `SHQueryUserNotificationState`による自動の退避 | 枠なし全画面のゲームが`QUNS_BUSY`を返すかは未確認です |
| 最前面の小窓の画面キャプチャへの映り込み | 配信や画面共有に映ると**推定**しています。実測していません。設定の「ほかの窓に隠れてよい」が受け皿になります |
| マルチモニタとDPIの変更 | **Per-Monitor V2が効いていることは確認済み**です（`AreDpiAwarenessContextsEqual`で判定。`GetProcessDpiAwareness`の列挙にはV2の値が無く、V1と区別できないため使えません）。ただし単一モニタでしか試していません。主モニタより左や上にモニタがある構成、モニタごとに違う倍率、`WM_DPICHANGED`と`AllowsTransparency`の組み合わせは未確認です |
| ClearTypeが無効になった状態の可読性 | **確認済み**（2026年9月9日）。Step 1の小窓を実機で見て、読めることを確かめました。レイヤードウィンドウを使う限りClearTypeは無効のままですが、いまのフォントの大きさと太さとコントラストで実用に足ります |
| 余白のクリックスルー | **確認済み**（2026年9月9日）。alphaが0の余白でクリックが下へ抜け、カード面は掴めることを実機で確かめました |
| 小窓の常駐時のメモリ | 起動から104秒の時点で、ワーキングセット114.1MB・プライベート62.2MB・子プロセス0個でした。`docs/stack.md`の「WPFは1プロセス40〜70MB」という見込みより大きく、**プライベートメモリで見れば範囲内**です。長時間の常駐での増加は未測定 |

## 運用上のリスク

- **署名**は技術選定では解けません。[開発環境とCI](development.md)の「署名と配布」を見てください
- **Open-Meteoの無料枠**は非商用の条件付きです。利用者が各自の任意の座標でポーリングすると、エッジのキャッシュのヒット率が下がり、上流の消費が増えます。利用者が増えた場合の見通しを別に立てる必要があります
- **Windows App SDK 2.x系の保守期限は2027年4月29日**です。2.4に上げても期限は延びません。1.x系のようにマイナー更新で期限が伸びる挙動とは違います。3.0への移行を年次の作業として計画に入れてください。新しいStableを月次で検知するだけのジョブをCIに1本置くと、駆け込みの移行を避けられます
- **`windows-latest`は予告なく更新されます**。2026年6月にWindows Server 2025とVisual Studio 2026へ移行済みです。再現性を重視するなら`windows-2025`のような明示のラベルに固定する選択肢があります
- **`.NET`をself-containedにするため、公式に「Not serviceable」の扱いになります**。`.NET`の脆弱性の修正は、アプリを出し直さないと利用者へ届きません。月次程度のリリース運用が前提になります
- **利用者単位のインストールは、複数の利用者がいる端末で重複します**。ランタイムも実行した利用者にだけ登録されます。共用のPCを想定に入れるなら考え直しが要ります
- **Inno Setupの条文は無償で商用も認めています**。ただし2025年8月12日の6.5.0以降、開発元が年間収益で5000米ドルを超える商用の利用者へ有償ライセンスの購入を求めています。非営利なら対象外ですが、寄付の収入も収益に算入されます

## 見積もりの不確実性

各Stepの日数と、移植の行数の見積もりは設計上の試算です。
実際に書いて計測したものではありません。
3割程度の振れは見込んでください。

[技術選定](stack.md)に載せたメモリーの数値は、1台での測定であり、他のアプリを代理の指標にしたものです。
FursuitWeatherのクライアント自体を測った値ではありません。
結論は「クリックスルー」「通知」「資産の流用」の3点で成り立っており、この3つは一次情報で確認済みです。
占有と配布の数値は補助的な根拠として扱ってください。

## 実装の残作業

- 小窓とトレイのUIが未実装です。3つの層のうち「常に表示する」層がまだありません

## リポジトリの残作業

テンプレートから作った直後の作業は済みました。
`scripts/setup.ps1`の実行、`.NET` SDKの導入、ブランチ保護の設定はすべて完了しています。

残っているのは次の1つです。

- `SECURITY.md`に非公開の連絡先を書いていません。Private vulnerability reportingは有効にしたため、GitHubの非公開の報告フォームは使えます

`package.json`の`name`と`description`、ルートの`README.md`の書き換えは済みました。
